# Определение эмоций по голосу — веб-приложение

Веб-сервис на FastAPI для распознавания эмоций по аудио: запись из браузера или загрузка файла, инференс обученной нейросети и показ вероятностей.

## Возможности
- 🎤 Запись голоса прямо в браузере
- 📁 Загрузка аудио файлов (WAV)
- 🤖 Классификация эмоций нейросетью
- 📊 Вывод вероятностей по всем классам
- 🎨 Готовый UI без дополнительной сборки

## Поддерживаемые эмоции
- Радость (happiness)
- Грусть (sadness)
- Злость (anger)
- Страх (fear)
- Отвращение (disgust)
- Энтузиазм (enthusiasm)
- Нейтрально (neutral)

## Установка
```bash
pip install -r requirements.txt
```

## Использование

### 1) Анализ датасета
```bash
python analyze_dataset.py
```

### 2) Обучение модели
- Аугментация: для каждой записи генерируется x10 вариантов (шум, питч-шифт, тайм-стретч, сдвиг, усиление).
- Длительность аудио нормализуется, признаки: MFCC, chroma_stft, mel, spectral contrast, tonnetz.

Старт обучения:
```bash
python train_model.py
```
По завершении появятся `emotion_model.h5` и `label_encoder.json`.

### 3) Запуск веб-приложения
```bash
python app.py
# или
uvicorn app:app --host 0.0.0.0 --port 8000 --reload
```
Открыть в браузере: `http://localhost:8000`

## Структура проекта (основное)
```
train/               # Аудио для обучения
test/                # Тестовые аудио
train.csv            # Метаданные обучающего набора
test.csv             # Метаданные тестового набора
analyze_dataset.py   # Аналитика датасета
train_model.py       # Обучение и сохранение модели
app.py               # FastAPI приложение + фронтенд
emotion_model.h5     # Сохранённая модель (после обучения)
label_encoder.json   # Метки классов (после обучения)
telegram_bot.py      # Телеграм-бот (док в TELEGRAM_BOT.md)
requirements.txt     # Зависимости
README.md            # Этот файл
```

## Технологии
- Backend: FastAPI
- ML: TensorFlow/Keras, scikit-learn
- Аудио: librosa, soundfile
- Frontend: HTML/CSS/JS (vanilla)

## Особенности модели
- Признаки: MFCC, Chroma STFT, Mel, Spectral Contrast, Tonnetz
- Архитектура: полносвязная сеть с dropout
- Регуляризация/обучение: Adam, early stopping, ReduceLROnPlateau

## Примечания
- Перед запуском веб-приложения нужна обученная модель (`emotion_model.h5`, `label_encoder.json`).
- WAV предпочтительный формат; запись из браузера требует доступа к микрофону.
